import { portableTextSegment } from '../portableText';

export const localePortableTextSegment = `
    fi[] {
        ${portableTextSegment}
    },
    ru[] {
        ${portableTextSegment}
    },
    en[] {
        ${portableTextSegment}
    }
`;
